import { keyframes } from 'styled-components';

export const keyframeIconScale = keyframes`
  0% {
    transform: scale(0);
  }

  60% {
    transform: scale(1.2);
  }

  100% {
    transform: scale(1);
  }
`;
